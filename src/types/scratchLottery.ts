export declare interface ScratchCanvas{
    name:string,
    leftPosition:string, //指定scratch圖層的位置，給ScratchLotteryCard元件層去指定( 可使用%或px )
    topPosition:string,  //指定scratch圖層的位置,給ScratchLotteryCard元件層去指定( 可使用%或px )
    width:number, //指定Scratch圖層的寬
    height:number //指定Scratch圖層的高
}